Let's a new project using django 4.2 LTS and for UI Bootstrap 5 + Django-Crispy-Forms and for database sqlite. and for charts use ApexCharts v5

Functional Requirments
I will create a expense month, and in that month I will upload the multiple csvs for that month. For the month we will show at top Income and Expense cards for the month
below it there will table, when I will upload multiple csvs it will populate into that table. if do multiple csvs it will just append the rows in the table.

The table will editable , and I will also manually assign the categories from dropdown available for each transaction in the row. and it will update on each change.
Every column will editable and auto save.


There will be a separte screen for Categories CRUD

Screen
1.  Home/Dashboard, which will show the each month transition and also suggest me some widgets or charts from ApexCharts with my chart. Will not implement Home widgets in the initial stage we will do later. but let's plan for it
2. Expense Month entry - CRUD for the single month
3. Categories. 
4. Login anf Signup. No email confirmation or 2FA, just email and password. very simple flow.

